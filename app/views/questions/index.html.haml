%h1 Listing questions

%table.table.table-
  %tr
    %th Xml
    %th Created at
    %th Tags
    %th
    %th

  - @questions.each do |question|
    %tr
      %td= question.xml
      %td= question.created_at
      %td= question.tags
      %td
        = link_to 'Show', question, :class => 'btn btn-mini'
        = link_to 'Edit', edit_question_path(question), :class => 'btn btn-mini'
        = link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini'

%br

.btn= link_to 'New Question', new_question_path
