%h3 Question

= link_to 'Edit', edit_question_path(@question)

%dl
  %dt XML
  %dd= @question.xml
  
  %dt Created
  %dd= format_date(@question.created_at)

  -@question.hydra_attributes.each do |key, val|
    -next if val.nil?
    %dt=key
    %dd=val