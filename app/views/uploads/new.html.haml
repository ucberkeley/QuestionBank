.span12
  %h3 Upload Customized Data
  %p Upload a CSV file with data about questions or users.

.span12
  = simple_form_for :upload, :url => create_upload_path, :html => {:multipart => true, :class => 'form-vertical'} do |f|
    %p 
      If you are uploading data about 
      %strong users, 
      include a column with the name
      %strong User ID.
    .form-inputs
      =f.input :upload_file, :as => :file
      =f.submit :submit, :value => "Upload CSV", :id => "upload_data_submit", :class => 'btn btn-primary'

.span12
  %h4 Custom Attributes
  %p Specify new attribute name and formats here for different data types here

.span5
  %h5 Question Attributes
  %table.table.table-striped
    %tr
      %th Column Name
      %th Data Type
    - @question_attributes.each do |attr|
      %tr
        %td=attr.name
        %td=attr.backend_type


.span5
  %h5 User Attributes
  %table.table.table-striped
    %tr
      %th Column Name
      %th Data Type
    - @user_attributes.each do |attr|
      %tr
        %td=attr.name
        %td=attr.backend_type

.span12
  =link_to 'Create New Attribute', new_attribute_path, :class => 'btn btn-primary'