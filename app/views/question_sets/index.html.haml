%h1 Listing question_sets

%table
  %tr
    %th
    %th
    %th

  - @question_sets.each do |question_set|
    %tr
      %td= link_to 'Show', question_set
      %td= link_to 'Edit', edit_question_set_path(question_set)
      %td= link_to 'Destroy', question_set, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Question set', new_question_set_path
